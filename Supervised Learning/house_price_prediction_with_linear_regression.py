# -*- coding: utf-8 -*-
"""Hourse_Price_Prediction with Linear Regression.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1PpDkCmCqzyhkEsR5Qpz8djrW8rfpegMH
"""

import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

df  = sns.load_dataset('tips')
df

df = pd.read_csv('USA_Housing.csv')
df

df.head()

df.info()

df.describe()

df.head()

df.columns

# 1. Feature Selection
X = df[['Avg. Area Income', 'Avg. Area House Age', 'Avg. Area Number of Rooms',
       'Avg. Area Number of Bedrooms', 'Area Population']]
y = df['Price']

X

y

# 2. Split dataset into train and test
from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(X,y,test_size = 0.3,random_state = 101)

X_train

X_test

# 3. Import and load model
from sklearn.linear_model import LinearRegression
model = LinearRegression()

model

# 4. Train Model
model.fit(X_train,y_train)

model

# 5. Calculate weight and bias
model.coef_

model.intercept_

coeff_df = pd.DataFrame(model.coef_, X.columns, columns=['Coefficient'])
coeff_df

# 6. Prediction
predictions = model.predict(X_test)
predictions

y_test

X_test

# 7. Validation and Evalulation
plt.scatter(y_test,predictions)
plt.plot(y_test,y_test,color='red')
plt.xlabel('Actual Values')
plt.ylabel('Predicted Values')
plt.show()

predictions

y_test

predictions[0]

y_test.iloc[0]

print('Predicted Price: ', predictions[0] )
print('Actual Price: ', y_test.iloc[0])

print('Predicted Price: ', predictions[-1] )
print('Actual Price: ', y_test.iloc[-1])

print('Predicted Price: ', predictions[50] )
print('Actual Price: ', y_test.iloc[50])

print('Predicted Price: ', predictions[10] )
print('Actual Price: ', y_test.iloc[10])

print('Predicted Price: ', predictions[100] )
print('Actual Price: ', y_test.iloc[100])